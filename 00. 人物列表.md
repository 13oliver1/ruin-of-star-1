---
cssclasses:
  - cards cards-align-bottom cards-2-1 table-50
---

```dataviewjs
dv.table(
    ["图片", "角色编号", "名字", "种族", "职业", "介绍"],
    dv.pages("#OC")
    .map(b => [
       b.photo ? `<img src="${b.photo}">`  : "👤", // 如果没有照片，使用表情符号 😄 📷
        b.角色编号,
        b.file.link,
        b.ethnicity,
        b.职业,   
        b.备注, 
        ])
);
```
